{%- comment -%}
  Per Set header.

  One burger on the left opens the full-screen menu (every collection and
  page lives there now); the logo sits dead centre; search, account,
  favourites and cart sit on the right. Three columns, always, at every
  width — no separate mobile/desktop toggle to keep in sync.
{%- endcomment -%}

<header class="ps-header" data-ps-header>
  <div class="ps-shell ps-shell--wide">
    <div class="ps-header__inner">

      <button
        class="ps-icon-btn ps-header__burger"
        type="button"
        data-ps-menu-toggle
        aria-expanded="false"
        aria-controls="ps-menu"
        aria-label="{{ 'sections.header.menu' | t | default: 'Menu' }}">
        {% render 'icon', name: 'menu', size: 21 %}
      </button>

      <a class="ps-logo" href="{{ routes.root_url }}" aria-label="{{ shop.name }} — {{ 'general.home' | t | default: 'Home' }}">
        {%- if section.settings.logo != blank -%}
          {%- assign logo_alt = shop.name | escape -%}
          {{
            section.settings.logo
            | image_url: width: 420
            | image_tag:
              loading: 'eager',
              widths: '140,210,280,420',
              sizes: '(max-width: 760px) 120px, 180px',
              alt: logo_alt,
              class: 'ps-logo__img'
          }}
        {%- else -%}
          {% render 'logo', width: 'clamp(88px, 11vw, 118px)' %}
        {%- endif -%}
      </a>

      <div class="ps-header__tools">
        <a
          class="ps-icon-btn ps-hide@sm"
          href="{{ routes.search_url }}"
          aria-label="{{ 'general.search.title' | t | default: 'Search' }}">
          {% render 'icon', name: 'search', size: 19 %}
        </a>

        {%- if shop.customer_accounts_enabled -%}
          <a
            class="ps-icon-btn ps-hide@sm"
            href="{{ routes.account_url }}"
            aria-label="{{ 'customer.account.title' | t | default: 'Account' }}">
            {% render 'icon', name: 'account', size: 19 %}
          </a>
        {%- endif -%}

        <a
          class="ps-icon-btn ps-hide@sm"
          href="{{ section.settings.favorites_url | default: '/pages/favourites' }}"
          aria-label="{{ 'general.favorites.title' | t | default: 'Favourites' }}">
          {% render 'icon', name: 'heart', size: 19 %}
          <span class="ps-cart-count" data-ps-fav-count aria-hidden="true">0</span>
        </a>

        <a
          class="ps-icon-btn"
          href="{{ routes.cart_url }}"
          data-ps-cart-link
          aria-label="{{ 'sections.header.cart' | t | default: 'Cart' }}">
          {% render 'icon', name: 'cart', size: 19 %}
          <span
            class="ps-cart-count{% if cart.item_count > 0 %} is-on{% endif %}"
            data-ps-cart-count>{{ cart.item_count }}</span>
        </a>
      </div>

    </div>
  </div>
</header>

{%- comment -%} Full-screen menu. Wipes open from the top like a cloth drawn over a table. {%- endcomment -%}
<div class="ps-menu" id="ps-menu" data-ps-menu aria-hidden="true">
  <div class="ps-shell ps-shell--wide ps-menu__top">
    <span class="ps-logo">{% render 'logo', width: 'clamp(88px, 11vw, 118px)' %}</span>
    <button
      class="ps-icon-btn"
      type="button"
      data-ps-menu-close
      aria-label="{{ 'accessibility.close' | t | default: 'Close' }}">
      {% render 'icon', name: 'close', size: 22 %}
    </button>
  </div>

  <div class="ps-shell ps-shell--wide ps-menu__body">
    <nav aria-label="{{ 'sections.header.menu' | t | default: 'Menu' }}">
      <ul class="ps-menu__list">
        {%- for link in section.settings.menu.links -%}
          {%- if link.links.size > 0 -%}
            <li class="ps-menu__item ps-menu__item--group">
              <details class="ps-menu__group">
                <summary class="ps-menu__group-toggle">
                  <span class="ps-menu__idx">{{ forloop.index | prepend: '0' | slice: -2, 2 }}</span>{{ link.title }}
                  <span class="ps-menu__chevron" aria-hidden="true">{% render 'icon', name: 'chevron-down', size: 18 %}</span>
                </summary>
                <ul class="ps-menu__sublist">
                  <li>
                    <a class="ps-menu__sublink" href="{{ link.url }}">
                      {{ 'collections.general.view_all' | t | default: 'Everything' }}
                    </a>
                  </li>
                  {%- for child in link.links -%}
                    <li>
                      <a class="ps-menu__sublink" href="{{ child.url }}">{{ child.title }}</a>
                    </li>
                  {%- endfor -%}
                </ul>
              </details>
            </li>
          {%- else -%}
            <li class="ps-menu__item">
              <a class="ps-menu__link" href="{{ link.url }}">
                <span class="ps-menu__idx">{{ forloop.index | prepend: '0' | slice: -2, 2 }}</span>{{ link.title }}
              </a>
            </li>
          {%- endif -%}
        {%- endfor -%}
      </ul>
    </nav>

    <aside class="ps-menu__aside">
      {%- if section.settings.menu_image != blank -%}
        <div class="ps-menu__figure ps-ratio ps-ratio--34">
          {{
            section.settings.menu_image
            | image_url: width: 900
            | image_tag: loading: 'lazy', widths: '400,600,900', sizes: '(max-width: 960px) 60vw, 30vw', alt: ''
          }}
        </div>
      {%- endif -%}
      {%- if section.settings.menu_note != blank -%}
        <p class="ps-menu__note">{{ section.settings.menu_note }}</p>
      {%- endif -%}
    </aside>
  </div>

  <div class="ps-shell ps-shell--wide ps-menu__foot">
    <a class="ps-link" href="{{ routes.search_url }}">{{ 'general.search.title' | t | default: 'Search' }}</a>
    {%- if shop.customer_accounts_enabled -%}
      <a class="ps-link" href="{{ routes.account_url }}">{{ 'customer.account.title' | t | default: 'Account' }}</a>
    {%- endif -%}
    <a class="ps-link" href="{{ section.settings.favorites_url | default: '/pages/favourites' }}">{{ 'general.favorites.title' | t | default: 'Favourites' }}</a>
    {%- if section.settings.instagram != blank -%}
      <a class="ps-link" href="{{ section.settings.instagram }}" rel="noopener" target="_blank">Instagram</a>
    {%- endif -%}
  </div>
</div>

{% stylesheet %}
  .ps-logo__img { width: auto; max-height: 40px; object-fit: contain; }

  /* The single burger. Visible at every width now, left column. */
  .ps-header__burger { justify-self: start; }

  /* A dropdown row inside the full-screen menu. The toggle is styled to
     match a plain .ps-menu__link exactly, but it is a <summary> rather than
     a link, so it opens the sublist instead of closing the whole overlay. */
  .ps-menu__group { position: relative; }
  .ps-menu__group-toggle {
    display: flex;
    align-items: baseline;
    gap: 0.5em;
    cursor: pointer;
    list-style: none;
    font-family: var(--ps-display);
    font-size: clamp(2.2rem, 9vw, 5rem);
    font-weight: 300;
    font-variation-settings: "SOFT" 40, "WONK" 1, "opsz" 144;
    line-height: 1.06;
    letter-spacing: -0.03em;
    transform: translateY(110%);
    transition: transform var(--ps-t-drape) var(--ps-ease-set), color var(--ps-t-fast) var(--ps-ease-swap);
    transition-delay: var(--ps-delay, 0ms);
  }
  .ps-menu__group-toggle::-webkit-details-marker { display: none; }
  .ps-menu.is-open .ps-menu__group-toggle { transform: translateY(0); }
  .ps-menu__group-toggle:hover { color: var(--ps-accent); }
  .ps-menu__group-toggle .ps-menu__idx {
    font-family: var(--ps-body);
    font-size: 0.24em;
    letter-spacing: 0.2em;
    vertical-align: super;
    color: var(--ps-ink-faint);
    margin-right: -0.2em;
  }
  .ps-menu__chevron {
    display: inline-flex;
    color: var(--ps-ink-faint);
    transition: transform var(--ps-t-fast) var(--ps-ease-swap);
  }
  .ps-menu__group[open] .ps-menu__chevron { transform: rotate(180deg); }

  .ps-menu__sublist {
    display: flex;
    flex-direction: column;
    gap: 0.1em;
    padding: 0.4em 0 0.6em 0.1em;
  }
  .ps-menu__sublink {
    display: block;
    font-family: var(--ps-body);
    font-size: var(--ps-step-1);
    letter-spacing: 0.01em;
    color: var(--ps-ink-soft);
    padding-block: 0.15em;
    transition: color var(--ps-t-fast) var(--ps-ease-swap);
  }
  .ps-menu__sublink:hover { color: var(--ps-accent); }

  .ps-menu__top {
    display: flex;
    align-items: center;
    justify-content: space-between;
    height: var(--ps-header-h);
  }
  .ps-menu__body {
    display: grid;
    grid-template-columns: 1.4fr 0.8fr;
    gap: var(--ps-s-xl);
    align-items: center;
    padding-block: clamp(1.5rem, 5vw, 4rem);
  }
  .ps-menu__aside {
    display: flex;
    flex-direction: column;
    gap: var(--ps-s-m);
  }
  .ps-menu__figure {
    border-radius: var(--ps-radius-l);
    overflow: hidden;
    opacity: 0;
    transform: translateY(28px);
    transition: opacity var(--ps-t-drape) var(--ps-ease-set) 420ms,
                transform var(--ps-t-drape) var(--ps-ease-set) 420ms;
  }
  .ps-menu.is-open .ps-menu__figure { opacity: 1; transform: none; }
  .ps-menu__note {
    max-width: 34ch;
    font-family: var(--ps-display);
    font-style: italic;
    font-size: var(--ps-step-1);
    line-height: 1.34;
    color: var(--ps-ink-soft);
  }
  .ps-menu__foot {
    display: flex;
    gap: var(--ps-s-m);
    flex-wrap: wrap;
    padding-block: var(--ps-s-m) var(--ps-s-l);
    border-top: 1px solid var(--ps-hairline);
  }
  @media (max-width: 860px) {
    .ps-menu__body { grid-template-columns: 1fr; align-items: start; }
    .ps-menu__aside { display: none; }
  }
{% endstylesheet %}

{% schema %}
{
  "name": "Header",
  "settings": [
    { "type": "image_picker", "id": "logo", "label": "Logo" },
    { "type": "link_list", "id": "menu", "label": "Menu", "default": "main-menu" },
    { "type": "url", "id": "favorites_url", "label": "Favourites page link" },
    { "type": "header", "content": "Menu overlay" },
    { "type": "image_picker", "id": "menu_image", "label": "Menu image" },
    {
      "type": "textarea",
      "id": "menu_note",
      "label": "Menu note",
      "default": "Each collection is a complete set of twenty-four or sixteen, made to set a table for six or four."
    },
    { "type": "url", "id": "instagram", "label": "Instagram URL" }
  ],
  "enabled_on": { "groups": ["header"] }
}
{% endschema %}
